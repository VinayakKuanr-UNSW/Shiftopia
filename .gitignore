# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.mcp.json

# Local env (secrets live in the deploy host, not the repo)
.env
.vercel
.env*

# Python build artifacts
__pycache__/
*.pyc

# Android / Capacitor local outputs.
# The android/ project itself IS tracked; these are generated by `cap sync`,
# Gradle, or the local SDK install and must not be committed.
*.apk
*.aab
android/.gradle/
android/build/
android/app/build/
android/local.properties
android/capacitor-cordova-android-plugins/
android/app/src/main/assets/public/
android/app/src/main/assets/capacitor.config.json
android/app/src/main/assets/capacitor.plugins.json
android/app/src/main/res/xml/config.xml

# Playwright E2E
/test-results/
/playwright-report/
/playwright/.cache/
e2e/.auth/
/coverage/

# Reference-repo working copy (the almond fork, cloned for comparison)
capstone-project-26t2-9900-t17b-almond/

# Claude Code agent worktrees — transient; never commit as gitlinks
.claude/worktrees/

# graphify: keep the graph.json data artifact (tracked by convention), but ignore
# machine-local pointers and the large regenerable HTML viewer
graphify-out/.graphify_root
graphify-out/.graphify_python
graphify-out/graph.html
