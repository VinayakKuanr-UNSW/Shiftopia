# Environment Variables
# Copy this file to .env and fill in the values

# =============================================================================
# SUPABASE CONFIGURATION (Required)
# =============================================================================

# Supabase project URL - Get from Supabase Dashboard > Settings > API
# Example: https://yourproject.supabase.co
VITE_SUPABASE_URL=

# Supabase anonymous/public key - Get from Supabase Dashboard > Settings > API
# This is safe to expose in the frontend bundle
VITE_SUPABASE_ANON_KEY=

# =============================================================================
# BUILD CONFIGURATION (Optional)
# =============================================================================

# Set to 'development' or 'production'
# Default: development
NODE_ENV=development

# =============================================================================
# ML / SHIFT SYNTHESISER SERVICE (Optional — required for labour forecasting)
# =============================================================================

# Base URL of the FastAPI ML service (POST /predict/demand)
# Default: http://localhost:8000 when running via docker-compose
VITE_ML_URL=http://localhost:8000

# =============================================================================
# ANDROID / CAPACITOR (Optional — native shell development only)
# =============================================================================

# Points the Android WebView at the Vite dev server instead of the assets copied
# into the APK, so web changes appear without a rebuild + reinstall.
#   http://10.0.2.2:8080      emulator -> host machine localhost
#   http://192.168.1.23:8080  physical device -> your LAN IP
# Run `npm run dev`, then `npm run android:sync:live` once after setting it.
# UNSET THIS before building anything you distribute, or the app will try to
# reach a dev machine that is not there.
CAP_SERVER_URL=

# The app is served from https://localhost inside the Capacitor shell, so a
# fetch to a plain-http local service (the optimizer on :5005, ML on :8000) is
# mixed content and Chromium drops it before it leaves the device — which looks
# exactly like the service being down. Set to true to allow it.
# Development only; leave unset for anything shipped.
CAP_ALLOW_MIXED_CONTENT=

# =============================================================================
# E2E TESTS (Optional — required to run `npm run test:e2e`)
# =============================================================================

# Credentials Playwright uses to sign in (e2e/auth.setup.ts). Use a shared
# non-production test account; in CI set these as repository secrets.
# Never commit real values.
E2E_EMAIL=
E2E_PASSWORD=

# Override the base URL under test. Defaults to http://localhost:8080, and the
# config auto-starts the dev server there.
E2E_BASE_URL=

# =============================================================================
# SENTRY (Optional — error monitoring)
# =============================================================================

# Public DSN — safe to expose in the bundle. Errors are reported only when set.
# Get from Sentry Dashboard > Project Settings > Client Keys (DSN).
VITE_SENTRY_DSN=

# Environment label shown on each event ("production", "staging", etc.).
# Falls back to Vite's MODE if unset.
VITE_SENTRY_ENVIRONMENT=

# Release identifier — typically the git SHA. Set in CI.
VITE_SENTRY_RELEASE=

# Sampling rates (0.0 – 1.0). Defaults: traces=0.1, replay=0, replay-on-error=1.
VITE_SENTRY_TRACES_SAMPLE_RATE=0.1
VITE_SENTRY_REPLAYS_SESSION_SAMPLE_RATE=0
VITE_SENTRY_REPLAYS_ON_ERROR_SAMPLE_RATE=1.0

# Build-time source-map upload (CI only — never commit these).
# Without these vars, the build still succeeds; uploads are skipped.
SENTRY_AUTH_TOKEN=
SENTRY_ORG=
SENTRY_PROJECT=

# =============================================================================
# NOTES
# =============================================================================
# - Never commit .env with real values to version control
# - The Supabase Service Role Key should NEVER be used in the frontend
# - Edge functions use SUPABASE_URL and SUPABASE_SERVICE_ROLE_KEY from Supabase's
#   built-in environment (automatically injected, do not set manually)
